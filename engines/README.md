# Engines

Adapters normalize multiple independent detectors into the Toolchecklink report.

Planned lanes:
- browser_network
- dom_performance
- texhub_adapter
- streamdownloader_adapter
- yt_dlp_adapter
- streamlink_adapter
- verifier

Keeping adapters independent lets several engines run in parallel and makes the JSON show exactly which engine succeeded.
